[project]
name = "ranker-eass"
version = "1.0.0"
description = "Automated student repo evaluation pipeline"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
evaluate = "scripts.run_evaluation:main"
